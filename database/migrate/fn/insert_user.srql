DEFINE FUNCTION fn::insert_user(
	$name: option<string>,
	$username: string,
	$email: string,
	$password: string,
) {
	RETURN CREATE user CONTENT {
		name: $name,
		username: $username,
		email: $email,
		password: $password,
	};
}
